# Experiments

This directory is reserved for experiment manifests, launch configs, and sweep-specific metadata.

The current scaffold uses `scripts/run_sweep.py` to generate a JSON manifest in `results/`. Once the Brax SAC runner exists, store per-cell launch specs here if you want explicit checked-in configs.
