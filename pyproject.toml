[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fine-tune-stability"
version = "0.1.0"
description = "Core utilities for a fine-tuning stability atlas in robotic RL."
readme = "README.md"
requires-python = ">=3.9"
authors = [{name = "Codex"}]
dependencies = []

[tool.setuptools]
packages = ["atlas"]
